import { expect } from 'chai'
import { ControllerFactor } from '../src/controller'
import { GameStateFactory } from '../src/game_state'

describe('controller', function() {
    it('standup', function(done) {
	
    })
    it('draw', function(done) {
    })
})
